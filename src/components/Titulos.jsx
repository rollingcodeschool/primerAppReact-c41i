const Titulos = ({nuevoTitulo}) => {
    // console.log(props.nuevoTitulo)

  return (
    <section>
      <h1>Primer App react</h1>
      <h2>{nuevoTitulo}</h2>
    </section>
  );
};

export default Titulos;
